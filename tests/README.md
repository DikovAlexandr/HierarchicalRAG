# Tests

Prioritize deterministic unit tests for answer normalization, EM/F1, context serialization, distractor injection, and configuration validation. Add small integration tests for dataset and model adapters without downloading large artifacts during the default test run.
