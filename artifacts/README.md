# Artifacts

Local model `checkpoints/` and retrieval `indexes/` are ignored by Git. Record their source or build command, revision, checksum, and compatible configuration; never commit weights or indexes.
