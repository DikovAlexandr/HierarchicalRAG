# Results

`runs/` contains generated predictions, logs, and traces and is ignored by Git. `summary.csv` is the reviewed, tracked index of aggregate results.

Add a summary row only after metric validation. Use an experiment ID that resolves to a configuration, and record failures rather than deleting inconvenient runs.
