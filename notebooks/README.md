# Notebooks

Use notebooks for exploration and visualization, not as the only implementation of loaders, metrics, retrieval, or model interfaces. Clear large outputs before committing and move reusable logic to `src/`.
