# Reports

- `pre-defense-content.md` is the maintained source for the future presentation.
- Put generated figures in `figures/` and final metric tables in `tables/`.
- Paper sources can be added under `paper/` after the proceedings template is copied from the temporary context.

Every reported number must reference an experiment ID and configuration. Keep narrative claims synchronized with `results/summary.csv`.
